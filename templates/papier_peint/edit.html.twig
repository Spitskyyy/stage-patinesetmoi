{% extends 'base.html.twig' %}

{% block title %}Edit PapierPeint{% endblock %}

{% block body %}
    <h1>Edit PapierPeint</h1>
    <div>
		<label>Images actuelles :</label>
		<div class="d-flex flex-wrap">
			{% for picture in papier_peint.pictures %}
				<div class="m-2 position-relative">
					<img src="{{ asset('uploads/images/' ~ picture) }}" width="100" class="rounded shadow">
					<input type="checkbox" name="delete_pictures[]" value="{{ picture }}">
					Supprimer
				</div>
			{% endfor %}
		</div>
	</div>

    {{ include('papier_peint/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_papier_peint_index') }}">back to list</a>

    {{ include('papier_peint/_delete_form.html.twig') }}
{% endblock %}
