<form method="post" action="{{ path('app_papier_peint_delete', {'id': papier_peint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ papier_peint.id) }}">
    <button class="btn">Delete</button>
</form>
