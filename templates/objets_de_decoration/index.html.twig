{% extends 'base.html.twig' %}

{% block title %}ObjetsDeDecoration index{% endblock %}

{% block body %}
    <h1>ObjetsDeDecoration index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Picture</th>
                <th>Detail</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for objets_de_decoration in objets_de_decorations %}
            <tr>
                <td>{{ objets_de_decoration.id }}</td>
                <td>{{ objets_de_decoration.title }}</td>
                <td>{{ objets_de_decoration.picture }}</td>
                <td>{{ objets_de_decoration.detail }}</td>
                <td>
                    <a href="{{ path('app_objets_de_decoration_show', {'id': objets_de_decoration.id}) }}">show</a>
                    <a href="{{ path('app_objets_de_decoration_edit', {'id': objets_de_decoration.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_objets_de_decoration_new') }}">Create new</a>
{% endblock %}
