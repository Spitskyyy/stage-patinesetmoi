<form method="post" action="{{ path('app_garniture_delete', {'id': garniture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ garniture.id) }}">
    <button class="btn">Delete</button>
</form>
