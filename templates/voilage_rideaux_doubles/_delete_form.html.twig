<form method="post" action="{{ path('app_voilage_rideaux_doubles_delete', {'id': voilage_rideaux_double.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voilage_rideaux_double.id) }}">
    <button class="btn">Delete</button>
</form>
