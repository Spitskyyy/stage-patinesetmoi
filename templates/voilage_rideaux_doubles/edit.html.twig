{% extends 'base.html.twig' %}

{% block title %}Edit VoilageRideauxDoubles{% endblock %}

{% block body %}
    <h1>Edit VoilageRideauxDoubles</h1>

    <div>
		<label>Images actuelles :</label>
		<div class="d-flex flex-wrap">
			{% for picture in voilage_rideaux_double.pictures %}
				<div class="m-2 position-relative">
					<img src="{{ asset('uploads/images/' ~ picture) }}" width="100" class="rounded shadow">
					<input type="checkbox" name="delete_pictures[]" value="{{ picture }}">
					Supprimer
				</div>
			{% endfor %}
		</div>
	</div>

    {{ include('voilage_rideaux_doubles/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_voilage_rideaux_doubles_index') }}">back to list</a>

    {{ include('voilage_rideaux_doubles/_delete_form.html.twig') }}
{% endblock %}
