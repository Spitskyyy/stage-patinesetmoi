<form method="post" action="{{ path('app_secteur_publique_monument_historique_delete', {'id': secteur_publique_monument_historique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secteur_publique_monument_historique.id) }}">
    <button class="btn">Delete</button>
</form>
