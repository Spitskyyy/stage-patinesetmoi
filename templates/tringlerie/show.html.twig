{% extends 'base.html.twig' %}

{% block title %}Tringlerie{% endblock %}

{% block body %}
    <h1>Tringlerie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tringlerie.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ tringlerie.title }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ tringlerie.pictures }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ tringlerie.detail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tringlerie_index') }}">back to list</a>

    <a href="{{ path('app_tringlerie_edit', {'id': tringlerie.id}) }}">edit</a>

    {{ include('tringlerie/_delete_form.html.twig') }}
{% endblock %}
