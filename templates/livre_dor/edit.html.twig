{% extends 'base.html.twig' %}

{% block title %}Edit LivreDor{% endblock %}

{% block body %}
    <h1>Edit LivreDor</h1>

    <div>
		<label>Images actuelles :</label>
		<div class="d-flex flex-wrap">
			{% for picture in livre_dor.pictures %}
				<div class="m-2 position-relative">
					<img src="{{ asset('uploads/images/' ~ picture) }}" width="100" class="rounded shadow">
					<input type="checkbox" name="delete_pictures[]" value="{{ picture }}">
					Supprimer
				</div>
			{% endfor %}
		</div>
	</div>

    {{ include('livre_dor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_livre_dor_index') }}">back to list</a>

    {{ include('livre_dor/_delete_form.html.twig') }}
{% endblock %}
