<form method="post" action="{{ path('app_livre_dor_delete', {'id': livre_dor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livre_dor.id) }}">
    <button class="btn">Delete</button>
</form>
