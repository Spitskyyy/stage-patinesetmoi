{% extends 'base.html.twig' %}

{% block title %}LivreDor{% endblock %}

{% block body %}
    <h1>LivreDor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livre_dor.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ livre_dor.title }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ livre_dor.picture }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ livre_dor.detail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_livre_dor_index') }}">back to list</a>

    <a href="{{ path('app_livre_dor_edit', {'id': livre_dor.id}) }}">edit</a>

    {{ include('livre_dor/_delete_form.html.twig') }}
{% endblock %}
