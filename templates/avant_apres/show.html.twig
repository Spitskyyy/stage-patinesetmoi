{% extends 'base.html.twig' %}

{% block title %}AvantApres{% endblock %}

{% block body %}
    <h1>AvantApres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avant_apre.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ avant_apre.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ avant_apre.image }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ avant_apre.detail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avant_apres_index') }}">back to list</a>

    <a href="{{ path('app_avant_apres_edit', {'id': avant_apre.id}) }}">edit</a>

    {{ include('avant_apres/_delete_form.html.twig') }}
{% endblock %}
