<form method="post" action="{{ path('app_fauteuil_dagrement_delete', {'id': fauteuil_dagrement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fauteuil_dagrement.id) }}">
    <button class="btn">Delete</button>
</form>
